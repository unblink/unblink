export default function ChatControls() {
  // Simplified controls for now - can be extended later
  return (
    <div class="flex gap-2 items-center">
      {/* Placeholder for future controls like web search toggle */}
    </div>
  );
}
